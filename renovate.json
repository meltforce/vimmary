{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Headless Renovate (no PR workflow): minors and patches land directly on main; a broken bump fails the build in build-push-deploy@v3 and no image is pushed. Preset: homelab/default.json.",
  "extends": ["local>meltforce.net/homelab"],
  "rangeStrategy": "pin",
  "postUpdateOptions": ["gomodTidy"],
  "packageRules": [
    {
      "description": "All npm deps share one package-lock.json — bumped individually they produce mutual merge conflicts and stall as open PRs (this happened with 3 of them on 2026-08-01). One branch for all non-major JS updates means one lockfile change and no conflict. Majors stay individual and dashboard-gated.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "npm non-major"
    }
  ]
}
